app:
  flask_env: "development"
  flask_app: "app"
  rebuild_app: true
  debug: true
database:
  type: "postgresql"
  host: "localhost"
  port: 5432
  user: "postgres"
  password: ${DB_PASSWORD}
  db_name: "finiautoma_dev"
  schema: "data"
default:
  user_id: 0
  user_name: "Default"
  